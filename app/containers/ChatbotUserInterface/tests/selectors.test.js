// import { fromJS } from 'immutable';
// import { selectChatbotUserInterfaceDomain } from '../selectors';

describe('selectChatbotUserInterfaceDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
